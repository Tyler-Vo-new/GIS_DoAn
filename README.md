# 🏠 Dự án GIS

## 📌 Hướng dẫn cài đặt và chạy dự án

### 1. Clone repository

Clone git
`git clone https://github.com/Tyler-Vo-new/GIS_DoAn`

### 2. StartUp

- `cd GIS` để vào thư mục GIS
- `npm install` để thực hiện cài đặt các dependencies
- `npm run dev` để mở cổng localhost và trình duyệt

### 3. Cấu trúc cơ bản

- `src/` : chứa mã nguồn React
- `public/` : chứa tài nguyên tĩnh
- `package.json` : quản lý dependencies và scripts
- `Components` chứa các file .jsx thành phần có thể sử dụng ở nhiều trang khác nhau
- `Pages` chứa các file .jsx thể hiện từng trang theo đề mục của NavBar
- `Styles/Components` chứa các file CSS của các components
- `Styles/Pages` chứa các file CSS của các trang

### 4. Công nghệ sử dụng

- React + Vite
- React Router
- CSS
